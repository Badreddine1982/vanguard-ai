# Experiments

Research prototypes remain isolated here until they pass the integration gates.

Current candidates include the existing MCTS/T5/RL line of work from the
project library. They may depend on experimental model tooling and are not
assumed to define the VANGUARD runtime contract.

Promotion path:

`experiment -> benchmark -> verification -> architecture compatibility -> integration`
